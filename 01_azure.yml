---
- hosts: localhost
  connection: local
  vars:
    user: user4
  tasks:
  - name: Create resource group
    azure_rm_resourcegroup:
      name: "webserver_{{ user4 }}"
      location: northeurope
      tags:
          solution: "webserver_{{ user4 }}"
          delete: ansibletraining
    register: rg
  - debug:
      var: rg