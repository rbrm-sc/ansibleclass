---
- hosts: localhost
  connection: local
  vars:
    user: rbrm-sc
  tasks:
  - name: Create resource group
    azure_rm_resourcegroup:
      name: "webserver_{{ rbrm-sc }}"
      location: northeurope
      tags:
          solution: "webserver_{{ rbrm-sc }}"
          delete: ansibletraining
    register: rg
  - debug:
      var: rg